language: python
python:
  - "2.6"
  - "2.7"
before_script:
  - sudo pip install django django-reversion diff-match-patch --use-mirrors
  - sudo python setup.py install
script:
  - cd example && python manage.py test
